/**
 * 공용 사용 modal 컴포넌트
 * 기본적인 디자인과 우상단 x버튼을 이용한 모달 닫기버튼
 * 내부내용, leftBtnText, rightBtnText를 받아
 * 각각 사용하되
 * rightBtnText의 default값은 '취소'로 표기할 수 있도록 한다.
 */

import React from 'react';

const CommonModal = ({ rightBtnText, leftBtnText }) => {
  return (
    <div>

    </div>
  );
};

export default CommonModal;